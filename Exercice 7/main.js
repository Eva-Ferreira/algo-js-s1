let user = {
    firstName : " Enzo",
    lastName : " Bourguignon",
    Passion : " Overwatch",
    birthDate : " 2004",
}

let history = user.firstName + user.lastName + " a comme passion" + user.Passion + " et il est né en" + user.birthDate 

console.log(history)